const openSigninBtn1 = document.querySelector('[data-model-target]');

const openLoginBtn = document.querySelector('[data-login-target]');

const opensigninBtn = document.querySelector('[data-signin-target]')

const closeSigninBtn = document.querySelector('[data-close-button]');

const closeSigninBtn2 = document.querySelector('[data-cross-button]');

const overlay = document.getElementById('overlay');

const signIn = document.getElementById('signin');

const login = document.getElementById('login');

const searchBtn = document.getElementById('searchBtn');

let input = document.getElementById('searchText');

const authors = Array.from(document.getElementsByClassName('author'));

const blog = Array.from(document.getElementsByClassName('home-article-search'));